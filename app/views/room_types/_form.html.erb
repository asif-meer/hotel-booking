<%= form_for(@room_type) do |f| %>
  <% if @room_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room_type.errors.count, "error") %> prohibited this room_type from being saved:</h2>

      <ul>
      <% @room_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :class => "text_field_width_200" %>
  </div>
  </br>
  <div class="field">
    <%= f.label :baserate %><br />
    <%= f.text_field :baserate %>
  </div>
  </br>
  <div class="field">
    <%= f.label :facilities %><br />
    <%= f.text_area :facilities %>
  </div>
  </br>
  <div class="actions">
    <%= f.submit 'Update', :class => "links_btn" %>
  </div>
<% end %>
